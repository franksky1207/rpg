name: Story Integrity

on:
  push:
    branches: [main]
    paths:
      - 'data.js'
      - 'worldmaps-*.js'
      - 'storydata-*.js'
      - 'story.css'
      - 'storyintegrity.js'
      - 'storyui.js'
      - 'storymigration.js'
      - 'storyprogress.js'
      - 'storyrecordtabs.js'
      - 'storyruntimeintegrity.js'
      - 'gmstorytest.js'
      - 'ui.js'
      - 'combatcore.js'
      - 'battlepipeline.js'
      - 'index.html'
      - 'tests/story/**'
      - '.github/workflows/story-integrity.yml'
  workflow_dispatch:

jobs:
  story-integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Verify story integrity structure
        run: |
          test -f story.css
          test -f storyintegrity.js
          test -f storymigration.js
          test -f storyruntimeintegrity.js
          test -f tests/story/source-purity.js
          test -f tests/story/integrity.js
          test -f tests/story/flow.js
          test ! -e story-source-purity-ci.js
          test ! -e story-integrity-ci.js
          test ! -e story-flow-ci.js
          grep -q 'story.css?v=20260917-story-batch4' index.html
          grep -q 'storyintegrity.js?v=20260918-story-policy2' index.html
          grep -q 'storymigration.js?v=20260918-retire-backfill1' index.html
          grep -q 'storyruntimeintegrity.js?v=20260918-retire-backfill1' index.html
      - name: Run raw story source purity
        run: node tests/story/source-purity.js
      - name: Run story data integrity
        run: node tests/story/integrity.js
      - name: Run story flow regressions
        run: node tests/story/flow.js
