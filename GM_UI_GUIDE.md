# GM 介面設計規範

更新日期：2026-09-26

> 本文件用來固定 GM／管理介面的視覺、按鈕語意與目前區塊原則。未來新增 GM 功能時，優先依此規範決定位置與按鈕配色，避免同類功能不同色或退休功能被誤當現行範例。

## 一、按鈕配色規則

### 1. 一般管理操作：預設深色

用途：一般設定、狀態調整、非危險性管理操作。

例如：
- 指定等級
- 指定金幣／暗物質／暗能量
- 指定正式 VIP 積分
- 一般狀態同步／選擇
- 關閉管理模式

原則：若按鈕不屬於「立即執行／測試」、「產生／新增」、「危險／刪除」，就維持預設深色。

---

### 2. 立即執行／刷新／測試：藍色

用途：立即執行某個動作、刷新資料、進行 Debug 或測試。

例如：
- 套用副本資料
- 查看 Debug
- 特殊怪測試
- 懸賞戰測試
- 戰力基準測試
- 未來其他批次模擬／測試按鈕

原則：藍色表示「執行一個動作」，但不代表資料會被破壞。

---

### 3. 產生／新增：金橘色

用途：建立新物件、產生資料、增加內容。

例如：
- 產生裝備
- 未來產生怪物
- 未來新增測試物品

原則：金橘色表示「建立或新增東西」。不要把生成類按鈕設成紅色。

---

### 4. 危險／刪除／清空／明確關閉：紅色

用途：會造成資料損失、清除內容、不可逆／高風險操作，或目前 GM 開關中明確用來關閉一項已啟用功能的 danger action。

例如：
- 重置整份存檔等真正破壞性操作
- 刪除／清除類 GM 操作
- 「關閉背景戰鬥」
- 「關閉主線鎖血」

原則：紅色不要拿來表示一般執行或一般產生；若只是普通設定切換且現行 UI 已有專門語意，依該區塊正式樣式維持一致。

---

## 二、快速判斷

```text
深色 = 一般管理
藍色 = 執行／刷新／測試／開啟功能
金橘 = 產生／新增
紅色 = 危險／刪除／清空／明確關閉已啟用功能
```

## 三、目前管理頁重要順序

目前正式 GM「管理」頁頂部的重要區塊順序固定為：

1. 背景戰鬥
2. 主線鎖血
3. 戰鬥速度
4. 其餘一般管理與資料管理區塊

新增 GM 功能時不得隨意把其他區塊插到這三者之間；若要調整排序，需先重新讀 current `main` 並依使用者明確要求修改。

### 背景戰鬥

- 玩家介面沒有開關，只能由 GM 管理控制。
- 本機裝置＋登入帳號分離保存，不寫角色 save／Cloud Save。
- 開啟按鈕使用藍色；關閉按鈕使用 danger。

### 主線鎖血

- 玩家介面沒有開關，只能由 GM 管理控制。
- 本機裝置＋登入帳號分離保存，不寫角色 save／Cloud Save／GM JSON。
- 只作用於銀河／宇宙正式主線與正式主線 context 的特殊怪。
- 不作用於副本、災厄、回顧戰或 GM 測試。
- 開啟按鈕使用藍色；關閉按鈕使用 danger。

## 四、目前介面套用方向

| 功能 | 顏色 |
|---|---|
| 指定等級／資源／VIP積分 | 深色 |
| 套用一般管理狀態 | 深色 |
| 戰力基準／特殊怪／懸賞等測試 | 藍色 |
| 開啟背景戰鬥 | 藍色 |
| 關閉背景戰鬥 | 紅色／danger |
| 開啟主線鎖血 | 藍色 |
| 關閉主線鎖血 | 紅色／danger |
| 產生裝備 | 金橘色 |
| 關閉管理模式 | 深色 |

## 五、已退休範例不得再使用

下列項目曾出現在舊版 GM UI Guide，但目前已不是正式 GM 介面的現行範例，因此從正式範例表刪除：

- 「補滿 HP」
- 「清空背包」

歷史需求可從 Git 記錄查詢；不要因歷史文件曾出現就重新加回介面。

## 六、未來擴充原則

- 新增 GM 按鈕前先依「功能語意」判斷類別，再決定顏色。
- 不因為某個功能很重要就任意使用紅色。
- 同類型按鈕應使用一致顏色。
- 新 GM 區塊優先遵守現行 Hub 排序與 owner，不另做第二套管理 UI。
- GM 測試／預覽不得污染正式 save。
- 裝置偏好類 GM 開關若需要帳號隔離，優先沿用現有 device preference helper，而不是另寫重複 localStorage helper。
- 已退休功能不得繼續作為「目前介面」範例；歷史需求以 Git 記錄查詢，不在正式規範中保留。
